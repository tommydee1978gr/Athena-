# Core web app — install this first for local dev (`pip install -r requirements-core.txt`).
# Boots the FastAPI backend, auth, permissions, encrypted vault, all integrations,
# and the CLIProxyAPI brain client. No voice yet — see requirements-voice.txt.
fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1
python-multipart==0.0.32
cryptography==50.0.0
argon2-cffi==25.1.0
pydantic==2.13.4
Pillow==12.3.0
numpy==2.5.1
apscheduler==3.11.3
mcp==2.0.0
