# Changelog

Ένα entry ανά ουσιαστική αλλαγή/εύρημα — ώστε να μη χάνεται τίποτα σε memory gap μεταξύ sessions. Βλ. και `ATHENA.md` (κανόνες) και `docs/` (βαθύτερα τεχνικά σημειώματα).

## 2026-08-08

- **Live: κλείδωμα Τάσου ενεργό και στα δύο συστήματα.** ATHENA: `restrict_to_room=1` στο DB του Τάσου, verified live μέσω `is_restricted_to_room()` (True) — redeploy έγινε στο HEAD (`9160fcf`) μέσω SSH `docker rm`/`docker run` (ο Tommy το προσπάθησε αρχικά μόνος του αλλά δεν είχε τρέξει ακόμα, ξανατρέχτηκε). Επιπλέον αφαιρέθηκε το `family.locations.read` capability ειδικά από τον λογαριασμό του Τάσου (per-user override, όχι αλλαγή στο child role default — η Ελένα διατηρεί το δικαίωμα) μετά από ρητό αίτημα Tommy "να μην μπορεί να βλέπει που είμαστε, μόνο οι ενήλικοι" — αυτό μπλοκάρει το `/api/family/locations` endpoint άμεσα, ανεξάρτητα από το page-level κλείδωμα.
- **Home Assistant: νέο container `ha-fusion`** (`ghcr.io/matt8707/ha-fusion`, SSH `docker run` στο Unraid, port 5050, `HASS_URL` env var — χρειάστηκε directly, όχι post-launch UI config, root cause ενός πρώτου boot crash "Proxy target could not be determined"). Ο Tommy το ζήτησε ρητά ("βρες το ha-fusion") μετά από απογοήτευση με το πόσο άσχημο/λειτουργικό ήταν το stock HA Lovelace dashboard (`/room.jpg` δεν έδειχνε φωτογραφία, το τηλεκοντρόλ είχε 4 "σπασμένα" κουμπιά που ήταν στην πραγματικότητα άδειες spacer κάρτες σε λάθος grid, η λάμπα δεν είχε καθόλου πρόσβαση σε χρώμα επειδή το tap_action ήταν κλειδωμένο σε `none`). Νέο view "Δωμάτιο Τάσου" (`picture_elements` card) πάνω σε πραγματική φωτογραφία του δωματίου (`tasos-room.jpg`, δόθηκε από τον Tommy, hosted στο `http://192.168.1.2:8123/local/tasos-room.jpg` μέσω του ήδη υπάρχοντος HA `/config/www`) με 3 `state-icon` στοιχεία (διακόπτης/λάμπα/τηλεόραση) τοποθετημένα πάνω στα πραγματικά σημεία της φωτογραφίας, δεμένα στα σωστά entities. Σημαντικό reverse-engineering εύρημα (minified JS bundle, `PictureElementsConfig` chunk): το top-level `image:` config key στο `picture_elements` item δεν αρκεί μόνο του για rendering λόγω Svelte reactivity quirk — χρειάζεται τουλάχιστον ένα element στο `elements[]` array να προστεθεί/αλλάξει για να ξεκινήσει σωστά το re-render· «Add New Image» στο toolbar προσθέτει ένα raw **Konva.js image element** (attrs: x/y/width/height/scaleX/scaleY/src) με ένα απλό **URL text field** (`id="src"`) — καμία native file-picker εμπλοκή τελικά, το αρχικό μου συμπέρασμα ότι μπλόκαρε σε native OS dialog ήταν λάθος διάγνωση (το πραγματικό πρόβλημα ήταν stale element refs από επαναλαμβανόμενα `find()` calls). State-icon elements δένονται με απλό `entity_id` πεδίο στο πάνω toolbar. Verified ζωντανά μέσω raw config readback (`docker exec ha-fusion cat /app/data/dashboard.yaml`) πριν δηλωθεί έτοιμο.
- **Κλείδωμα λογαριασμού στο `/room` μόνο ("kiosk mode")** — ο Τάσος ζήτησε ρητά να μπορεί ο λογαριασμός του να βλέπει ΜΟΝΟ τη σελίδα δωματίου, τίποτα άλλο στην ATHENA. Νέα στήλη `restrict_to_room` στο `user_room_devices` (ήδη προστέθηκε νωρίτερα σήμερα, idempotent migration μιας και ο πίνακας ήταν ήδη live), νέο `set_room_restriction()`/`is_restricted_to_room()` στο `app/room.py`. Νέο middleware `room_only_gate` στο `app/main.py` — mirroring το `persona_wizard_gate`: κάθε GET από χρήστη με `restrict_to_room=1` ανακατευθύνεται σε `/room`, με εξαίρεση μόνο `/room` ίδιο, static assets, `/api/*`, `/welcome`, `/login`, `/logout`, `/health`. Νέα admin σελίδα `/admin/users/{id}/room` (link από το `/admin/users/{id}/edit`) για ανάθεση συσκευών (switch/light/tv entities) ΚΑΙ toggle του κλειδώματος — μέχρι τώρα η ανάθεση συσκευών γινόταν μόνο χειροκίνητα μέσω DB, τώρα υπάρχει UI. Verified end-to-end τοπικά (child χρήστης με restrict ενεργό ανακατευθύνεται σωστά, `/room` φορτώνει κανονικά, οι εξαιρέσεις μένουν προσβάσιμες) πριν το commit — ένα bug βρέθηκε ΚΑΙ διορθώθηκε στο ίδιο το test script μου (λάθος regex έπιανε το admin's edit-link αντί του child's), όχι στον κώδικα της ATHENA.
- **Νέα σελίδα "Το δωμάτιό μου" (`/room`)** — ο Τάσος ζήτησε μια όμορφη, κατανοητή σε παιδί καρτέλα για τον έλεγχο του δωματίου του, πριν μπούμε σε λεπτομέρειες device-by-device. Νέος πίνακας `user_room_devices` (admin αναθέτει ρητά ποια entities ανήκουν στο δωμάτιο κάθε χρήστη — 1:1 companion table), νέο module `app/room.py`, νέο capability `room.control` (admin/adult/child το έχουν by default). Μεγάλα κουμπιά: switch on/off (ο φυσικός διακόπτης — χωρίς αυτόν η λάμπα δεν έχει καν ρεύμα, οπότε **η ίδια η λάμπα δεν έχει δικό της on/off**, μόνο χρώμα/φωτεινότητα/effects, όπως το ζήτησε ρητά ο Tommy), 9 χρωματιστά swatches + slider φωτεινότητας + 3 effect κουμπιά (Πάρτι/Χαλάρωσε/Καθαρό) για τη λάμπα, πλήρες τηλεκοντρόλ (D-pad σταυρός, HOME/BACK, ένταση) για την τηλεόραση. Καμία εντολή δεν περνάει από το `propose_confirmed_action` — εκτελείται αμέσως, ίδια λογική με το autonomous Emby/Asterisk (ο χρήστης χειρίζεται το ΔΙΚΟ του δωμάτιο, όχι κάτι που προτείνει η AI), αλλά κάθε ενέργεια περιορίζεται αυστηρά στα entities που έχει ρητά αναθέσει ο admin σε αυτόν τον συγκεκριμένο χρήστη — ποτέ αυθαίρετο entity_id. Verified end-to-end τοπικά (άδεια/γεμάτη κατάσταση, capability gating) πριν το commit.
- **First-login wizard για το persona** (`/welcome`) — ο Τάσος ήταν ρητός: δεν ήθελε να χρειάζεται να ξέρει ότι υπάρχει το `/voice` για να ρυθμίσει όνομα/φωνή/avatar, έπρεπε να εμφανίζεται αυτόματα. Νέο `configured` column στο `user_personas` (idempotent `ALTER TABLE` migration, μιας και ο πίνακας ήταν ήδη live). Νέο middleware `persona_wizard_gate` στο `app/main.py` — **κάθε** GET request από συνδεδεμένο χρήστη με μη-ρυθμισμένο persona ανακατευθύνεται στο `/welcome`, όχι μόνο η αρχική σελίδα (whitelist μόνο για `/welcome` ίδιο, static assets, `/api/*`, `/login`, `/logout`, `/health`) — δεν μπορεί να το προσπεράσει πηγαίνοντας οπουδήποτε αλλού. Ο χρήστης είτε ολοκληρώνει το wizard είτε πατάει ρητά "Παράλειψη" (και τα δύο σημειώνουν `configured=1`). Το picker UI (φωνή/avatar) βγήκε σε shared helper (`_persona_picker_fields`) ώστε να μη διπλασιάζεται κώδικας ανάμεσα σε `/welcome` και `/voice`. Χρειάστηκε να ενημερωθούν τα test fixtures (`bootstrap()`/`add_child()`) να σημειώνουν `configured=True` ώστε τα υπόλοιπα, άσχετα tests να μη μπλοκάρονται στο wizard — και προστέθηκε νέο dedicated test για το ίδιο το gating behavior.
- **Προσωπικό persona ανά χρήστη (όνομα, προσωπικότητα, φωνή, avatar)** — ζητήθηκε από τον Τάσο μετά το κλείσιμο Discord/Snapchat integrations (και τα δύο αδιέξοδα: Discord δεν γίνεται αόρατο bot χωρίς ToS παραβίαση, Snapchat δεν έχει καθόλου public API). Νέος πίνακας `user_personas` (1:1 companion table, ίδιο μοτίβο με `voiceprints`/`location_consent`), νέο module `app/persona.py`. Το `ask()` system prompt (`app/orchestrator.py`) τώρα χρησιμοποιεί το προσωπικό όνομα του χρήστη αντί για πάντα "ATHENA", και προσθέτει τη σημείωση προσωπικότητας αν υπάρχει. Το TTS (`/api/voice/tts` στο `main.py`, `_speak_back` στο `satellite.py`) χρησιμοποιεί το προσωπικό voice_id του χρήστη. Φωνές ΚΑΙ avatar εικόνες είναι **admin-curated λίστες** (έως 5 έκαστη), όχι ελεύθερο πεδίο — ένα παιδί διαλέγει από έτοιμες επιλογές, δεν επικολλά αυθαίρετο voice ID/URL. Δημιουργήθηκαν 4 avatar εικόνες (αγόρι/κορίτσι/άντρας/ρομποτάκι) μέσω του ήδη υπάρχοντος δωρεάν `generate_image` της ATHENA (GPT Image/CLIProxyAPI, καμία επιπλέον χρέωση) — αποθηκευμένες σαν static assets στο `ui/persona-avatars/`, ίδιο μοτίβο με το υπάρχον `avatar.jpg`. Verified end-to-end τοπικά (set/get/update persona) πριν το commit.
- **Δυναμικές κάρτες απάντησης στο `/graph`** — αντί για πάντα-ίδιο πεζό κείμενο, όταν η ερώτηση απαντιέται μέσω ενός εργαλείου που η ATHENA ήδη ξέρει να "δείξει" καλύτερα (υγεία/email/σπίτι), εμφανίζεται μια δομημένη κάρτα φτιαγμένη τη στιγμή εκείνη, αντί για το κείμενο. Backend: το `ask()` (`app/orchestrator.py`) τώρα κρατάει `tool_results` (ό,τι εργαλείο κλήθηκε + το πραγματικό structured αποτέλεσμά του, σε όλους τους γύρους tool-calling του turn) — πριν πετιόταν μετά το feed-back στο μοντέλο, τώρα ταξιδεύει μέχρι το frontend μέσω του `/api/ask` response. Frontend: νέο `CARD_RENDERERS` registry στο `app.js` (`health_summary`→κάρτα υγείας, `gmail_search`→κάρτα email, `homeassistant_states`→κάρτα σπιτιού), σχεδιασμένο ώστε νέες κάρτες να προστίθενται χωρίς άλλη αλλαγή. Πέφτει αυτόματα πίσω στο απλό κείμενο αν κανένα tool_result δεν ταιριάζει ή αν το εργαλείο απέτυχε. Verified τοπικά με ρεαλιστικά δεδομένα (ίδιο σχήμα με τα πραγματικά, ήδη ζωντανά επιβεβαιωμένα, tool outputs) πριν το commit.
- **Νέο: προσωπικά δεδομένα υγείας/fitness (Health Connect Webhook ingestion).** Ερευνήθηκε πρώτα ζωντανά (Google search) πριν χτιστεί οτιδήποτε: το **Google Fit API** είναι αδιέξοδο (καμία νέα εγγραφή από Μάιο 2024, κλείνει τέλος 2026, η αντικατάστασή του Health Connect είναι on-device Android API, όχι cloud). Το **Huawei Health Kit** έχει πραγματικό cloud OAuth API αλλά απαιτεί η αιτούσα εφαρμογή να είναι δημοσιευμένη στο AppGallery — δεν πληρούται από ένα self-hosted server. Λύση: **Health Sync** app (Huawei Health → Android Health Connect) + **Health Connect Webhook** app (Health Connect → webhook POST, δωρεάν/self-hosted, υποστηρίζει custom auth headers) → νέο `/api/health/webhook` στην ATHENA. Νέοι πίνακες `health_metrics`/`health_webhook_tokens`, νέο `app/health.py` (ingest ανεκτικό σε partial/άγνωστους τύπους δεδομένων· `daily_summary()` για steps/sleep/heart rate/weight), νέο `health_summary` LLM tool πίσω από νέο `health.read` capability (ιδιωτικό ανά χρήστη, όχι κοινόχρηστο στην οικογένεια — ίδιο μοτίβο με memory.private), νέα κάρτα στο `/integrations` για δημιουργία/ανάκληση token. Verified end-to-end τοπικά πριν το commit (ingest + summary με πραγματικό-σχήμα payload).
- **3 πραγματικά voice bugs βρέθηκαν και διορθώθηκαν** από αναφορά του Tommy ("τρομερά αργή απόκριση", "δεν καταλαβαίνει καλά", "μιλάει ξανά πριν τελειώσει, ακούγεται διπλή φωνή") — όλα επιβεβαιωμένα ζωντανά πριν το fix, όχι θεωρητικά:
  1. *Αργή απόκριση*: μέτρηση πραγματικών χρόνων από `docker logs -t` σε ζωντανές συνομιλίες έδειξε 9-39 δευτερόλεπτα ΜΟΝΟ για το TTS (ElevenLabs) βήμα, ενώ το ίδιο το `ask()` ήταν γρήγορο (~1-2s). Direct timing test (`elevenlabs_synthesize`) επιβεβαίωσε ότι ο χρόνος synthesis είναι σχεδόν γραμμικός στο μήκος κειμένου (~30ms/char) — μεγάλες, αναλυτικές απαντήσεις σε φωνητική ερώτηση σήμαιναν δεκάδες δευτερόλεπτα σιωπή πριν ακουστεί κάτι. Fix: νέο `voice: bool` flag στο `ask()` (`app/orchestrator.py`) — όταν η ερώτηση ήρθε φωνητικά (mic ή satellite wake word, όχι πληκτρολογημένη), το system prompt ζητάει ρητά 1-3 σύντομες προτάσεις αντί για αναλυτική απάντηση. Το `/api/ask` (`app/main.py`), το `ask()` του `app.js`, και το `_finish_command` του `satellite.py` περνάνε τώρα αυτό το flag.
  2. *"Δεν καταλαβαίνει καλά"*: το STT δεν έδινε ΠΟΤΕ `language="el"` σε κανένα σημείο του φωνητικού pipeline — ούτε το wake-word check στο `satellite.py` (`_transcribe_best_effort`), ούτε η πραγματική μεταγραφή εντολής στο `ui/app.js` (`/api/voice/stt`). Το Whisper έκανε auto-detect γλώσσας σε κοντά, συχνά θορυβώδη κλιπ 2-3 δευτερολέπτων — αναξιόπιστο, συχνά μάντευε λάθος γλώσσα. Fix: `language="el"` σταθερά πλέον και στα δύο σημεία· επιπλέον το `satellite.py` δεν διάβαζε ΚΑΘΟΛΟΥ το ήδη υπάρχον config (`wake_model`/`wake_language`/`stt_model` από το Integrations → Local voice runtime) — τώρα το `ws_voice_satellite` (`app/main.py`) το περνάει σωστά.
  3. *"Μιλάει ξανά πριν τελειώσει, διπλή φωνή"*: το state machine του satellite (`run_satellite_session`) δεν είχε καμία προστασία από barge-in — μόλις το `_finish_command` έστελνε τα TTS bytes (χωρίς να περιμένει να ΠΑΙΞΕΙ ο client το ήχο), η κατάσταση γύριζε αμέσως σε "listening" και ξεκινούσε πάλι wake-check πάνω στην ίδια της τη φωνή που ακόμα έπαιζε — αν παρερμηνευόταν σαν "Αθηνά", απαντούσε ξανά πάνω στην πρώτη απάντηση. Fix: το `_speak_back` επιστρέφει πλέον μια εκτίμηση διάρκειας ομιλίας (~14 chars/sec ελληνικά) και το state machine αγνοεί εντελώς εισερχόμενο ήχο μέχρι να περάσει αυτό το παράθυρο.
- **MCP OAuth (`720bbf4`) redeployed και επαληθευμένο ζωντανά** — `docker inspect` revision label == HEAD, `/health` OK, τα δύο MCP server rows (Higgsfield/OpenArt) επιβίωσαν το redeploy (persistent volume). `list_servers()` επιβεβαιωμένο ζωντανά μέσα στο container: και οι δύο δείχνουν `oauth_status: not_applicable` γιατί ο browser-based consent step δεν έχει γίνει ακόμα — εκκρεμεί από τον Tommy (login στο δικό του Higgsfield/OpenArt account μέσω `/mcp` → "Σύνδεση OAuth", δεν αυτοματοποιείται μέσω SSH/curl).

## 2026-08-07

- **Wake word: Jarvis (Porcupine, English-only) → πάντα-ενεργό Ελληνικό "Αθηνά"** (`889d04c`). Το Porcupine δεν υποστηρίζει Ελληνικά· αντικαταστάθηκε με το ήδη υπάρχον satellite protocol (`app/satellite.py`, faster-whisper, wake phrase ήδη default "Αθηνά") συνδεδεμένο τώρα και στο browser tab μέσω νέου cookie-auth path στο `/ws/voice/satellite`. Το 👂 στο `/graph` ξεκινάει να ακούει αυτόματα στο load — όχι πια κουμπί για arm. Vendor bundles (`porcupine-web.js`, `web-voice-processor.js`) αφαιρέθηκαν.
- **Κανόνας τεκμηρίωσης deployment** (`f64a7d7`): προστέθηκε ρητός κανόνας στο `ATHENA.md` — ποτέ "live/deployed" χωρίς SSH επαλήθευση (`docker inspect` revision label + `/health`). Καταγράφηκε ως [issue #7](https://github.com/tommydee1978gr/Athena-/issues/7) το περιστατικό που το προκάλεσε (live container 9 commits πίσω, CI runner-acquisition-failure).
- **Πλήρης χάρτης αρχιτεκτονικής** — διάβασμα ολόκληρου του `app/` (6.7k γραμμές) και artifact με διαγράμματα (ask() tool-loop, confirmation tiers, permission model, integrations, schema): https://claude.ai/code/artifact/babc3f30-2eaa-401d-8b3b-80c9f040bb8d
- **CLIProxyAPI runtime investigation** — βλ. `docs/cliproxy-runtime.md`. Μέσα από το πραγματικό `/management.html` UI (όχι μόνο raw config JSON, που αποδείχτηκε παραπλανητικό): cooldown-after-failure είναι ήδη ενεργό ("Disable Cooling" = off). Ο μηχανισμός για πάντα-ενεργό Claude thinking εντοπίστηκε (Payload Configuration → Default Raw Rules, scoped σε `claude-*` μοντέλα) αλλά δεν εφαρμόστηκε ακόμα — εκκρεμεί έλεγχος `max_tokens`/`temperature` compatibility στο `app/orchestrator.py`. Επίσης επιβεβαιώθηκε: **image generation ήδη διαθέσιμο** μέσω `/v1/images/generations` του CLIProxyAPI πάνω στην υπάρχουσα ChatGPT/Codex OAuth σύνδεση — καμία ATHENA tool δεν το εκμεταλλεύεται ακόμα.
- **Απορρίφθηκε OpenRouter ως εναλλακτικός εγκέφαλος** — αυστηρά API-key/pay-per-token, παραβιάζει τον κανόνα "όχι API keys". Το image generation που ήθελε ο Tommy καλύπτεται ήδη δωρεάν από το υπάρχον setup (βλ. παραπάνω). Σημειώθηκε επίσης ότι το `apikey.fun` (sponsor του CLIProxyAPI project) είναι τρίτος paid API relay — να μην χρησιμοποιηθεί ως credential source.
- **Gap analysis: 3 requirement docs vs κώδικας** — `routes and routines απαιτησεις/` (everyday routing, ρουτίνες σπιτιού, creative workflow) χαρτογραφήθηκαν πλήρως έναντι του πραγματικού κώδικα. 28 requirements: 9 υπάρχουν, 8 μερικώς, 11 λείπουν. Artifact: https://claude.ai/code/artifact/41fdaabf-1bcc-4986-b3d3-baa656ec42a2 . Θεμελιώδες εύρημα: το per-room/per-user permission model λείπει εντελώς (επηρεάζει HA scoping και PBX extensions μαζί).
- **Home Assistant συνδέθηκε στο ATHENA** — `ha_base_url`/`ha_token` ρυθμίστηκαν (ο υπάρχων χρήστης `thomas` στο HA δεν είχε γνωστό password στον Tommy· reset μέσω `docker exec HomeAssistant hass --script auth -c /config change_password thomas <new>`, ίδιο μοτίβο ανάκτησης με το ATHENA admin password — greenfield HA credentials, previous session τα είχε φτιάξει χωρίς να τα μεταφέρει στον Tommy). Επιβεβαιωμένο end-to-end μέσω `/api/homeassistant/states`. Το ίδιο το HA είναι σχεδόν άδειο install — καμία πραγματική συσκευή/area ορισμένη ακόμα, ο Tommy τις προσθέτει τώρα.
- **4 λογαριασμοί οικογένειας στην ATHENA** — Ελένα/Τάσος (child), Δήμητρα (adult), μαζί με τους 6 HA users (thomasd/dimitra/tasos/elena + 2 οχήματα: rx8, saab) που έφτιαξε ο Tommy για per-person tracking/permissions.
- **`/api/ask` γίνεται streaming** (`1f60ca4`) — ρίζα του "αργεί αισθητά" ήταν ότι κανένα κείμενο δεν εμφανιζόταν μέχρι να τελειώσει ολόκληρος ο tool-calling loop. Τώρα: `app/cliproxy.py` έχει `chat_completions_stream()` (SSE), `app/orchestrator.py` το `_stream_chat_with_failover()` προωθεί text deltas ζωντανά μέσω νέου `on_delta` callback στο `ask()`, το `/api/ask` το στέλνει σαν newline-delimited JSON, και το `ui/app.js` το καταναλώνει προοδευτικά.
- **Claude thinking mode ενεργό μόνιμα** — δοκιμάστηκε πρώτα μέσω του CLIProxyAPI management UI (Payload Configuration → Default/Override Raw Rules) αλλά βρέθηκε **bug στο ίδιο το UI**: ό,τι rule φτιάχνεις (μέσω Simple ή Advanced/Source Protocol) αποθηκεύεται πάντα σαν άδειο array στο raw YAML — επιβεβαιώθηκε στο Source tab. Λύση: μπήκε κατευθείαν στον κώδικα της ATHENA (`_stream_chat_with_failover`) — όταν `model_family(model) == "claude"`, βάζει `temperature: 1`, `max_tokens: 8192`, `thinking: {"type":"enabled","budget_tokens":2000}` (Anthropic απαιτεί temperature=1 με thinking ενεργό). Καμία εξάρτηση πλέον από το proxy panel για αυτό.
- **Νέα φιλοσοφία δρομολόγησης** (Tommy, 2026-08-07): Gemini = ελαφριά θέματα, Claude = οργανωτής (reasoning/planning), Codex/OpenAI = εργάτης (εκτέλεση). Το `_task_profile()` στο `cliproxy.py` ξαναγράφτηκε ανάλογα — νέα κατηγορία "light_quick" για σύντομες/απλές ερωτήσεις → Gemini, broadened "execution_work" keywords για το ChatGPT-ως-εργάτη.
- **DeepSeek εξετάστηκε** για ειδική χρήση σε research tasks — υποστηρίζεται από το CLIProxyAPI αλλά **όχι μέσω OAuth login** (μόνο API key, έστω πολύ φθηνό). Τεχνικά εκτός του "όχι API keys" κανόνα· μπήκε σε backlog για συνειδητή απόφαση, δεν υλοποιήθηκε.
- **Bug βρέθηκε μέσω live testing στο `network_clients`**: `set_app_config("omada", ..., "system")` — το `"system"` δεν είναι έγκυρο user id, FOREIGN KEY constraint failure. Διορθώθηκε: το `site_id` τώρα cache-άρεται in-memory μαζί με το access token (ίδιο lifetime), δεν χρειάζεται καθόλου persistence στη DB. Επιβεβαιωμένο end-to-end μετά το fix (πραγματική απάντηση: "26 συσκευές συνδεδεμένες").
- **HACS εγκαταστάθηκε στο Home Assistant** (official installer script μέσω `docker exec`, restart). Χρειάζεται ένα ακόμα βήμα από τον Tommy (GitHub device authorization) που δεν μπορεί να αυτοματοποιηθεί.
- **Νέο integration: ο ίδιος ο Unraid server, admin-only.** `unraid-api apikey --name ATHENA --create --roles VIEWER` στο ίδιο το Unraid (SSH) — δωρεάν, τοπικό, ενσωματωμένο GraphQL API (Unraid 7.3+). Επαληθεύτηκε ζωντανά: **το port 443 ακούει μόνο σε 127.0.0.1**, άχρηστο από containers· η πραγματική δουλειά γίνεται σε plain **http://192.168.1.2/graphql** (port 80) με `x-api-key` header — καθαρό workaround, όχι κάτι που φαίνεται στα επίσημα docs. Νέο `server_status` tool (array state/capacity, docker containers) πίσω από νέο `infra.read` capability (admin-only, ίδιο μοτίβο με το `network.*`). Το "διαχείριση αρχείων" ξεχωριστό θέμα — το SHARE resource του Unraid API είναι για ρυθμίσεις share, όχι περιεχόμενα αρχείων· η πραγματική λύση είναι πρόσθετα volume mounts (ίδιο μοτίβο με το `/projects`), εκκρεμεί απόφαση ποια shares.
- **Νέο integration: Omada Controller (δίκτυο σπιτιού), admin-only.** Client-credentials OAuth (Client ID/Secret δημιουργημένα στο ίδιο το Omada Controller → Settings → Platform Integration) — **δεν παραβιάζει τον κανόνα "όχι API keys"**, είναι τοπική συσκευή δικτύου σαν το HA/Emby, όχι cloud LLM. **Επαληθεύτηκε ζωντανά end-to-end** στο πραγματικό controller (`https://192.168.1.2:18043`) πριν μπει στον κώδικα — βρέθηκε και διορθώθηκε bug στο αρχικό μου implementation (το path είναι `/openapi/authorize/token`, όχι `/openapi/v1/authorize/token`). Δύο read tools (`network_clients`, `network_devices`, gated πίσω από νέο `network.read` capability, admin-only ρητά — αφαιρέθηκε από adult) και ένα confirm-first action (`network.client_action`: block/unblock/reconnect/disconnect, `network.control`). Πραγματικό site βρέθηκε: "Nea_Smirni".
- **OAuth 2.1 υποστήριξη στο MCP client** — Higgsfield (`https://mcp.higgsfield.ai/mcp`) και OpenArt (`https://mcp.openart.ai/mcp`) καταχωρήθηκαν σαν MCP servers, αλλά το πρώτο `refresh` γύρισε `tool_count: 0` — βρέθηκε (μέσω `docker exec ... python3 -c "..."` diagnostic, ίδιο μοτίβο με πριν) ότι και οι δύο απαιτούν OAuth, όχι static API key: `session.initialize()` γυρνούσε `401` με `WWW-Authenticate: Bearer resource_metadata="https://.../.well-known/oauth-protected-resource/mcp"`. Αντί για workaround (extract token μέσω `higgsfield auth login` CLI), χτίστηκε πλήρες OAuth 2.1 + PKCE(S256) + Dynamic Client Registration (RFC 7591) + discovery (RFC 9728/8414) στο `app/mcp_client.py` — μόνο του, χωρίς να ξαναχρησιμοποιηθεί το MCP SDK's `OAuthClientProvider` (σχεδιασμένο για CLI callback flows, δεν ταιριάζει στο request-driven FastAPI σχήμα της ATHENA)· αντ' αυτού χτίστηκε mirroring το ήδη υπάρχον Google/Spotify OAuth pattern του `app/integrations.py` (`store_oauth_state`/`_pkce_pair`/`_json_request`). Ροή: admin πατάει "Σύνδεση OAuth" στο `/mcp` → discovery+DCR αυτόματα (επιβεβαιωμένο ζωντανά και για τους δύο servers πριν μπει live, δίνει πραγματικά `client_id`) → redirect στη σελίδα έγκρισης του server → `/mcp/oauth/callback` κάνει token exchange και αποθηκεύει το token μέσα στο `mcp_servers.config_enc` (καμία αλλαγή schema — «oauth» sub-key), με αυτόματο refresh όταν λήξει. Η σελίδα `/mcp` δείχνει τώρα στήλη κατάστασης OAuth (συνδεδεμένο/εκκρεμεί/—) ανά server.
- **Unraid: ATHENA container recreate — λύθηκε το "third party" badge + το ξεπερασμένο image ταυτόχρονα.** Root cause: ο container δημιουργήθηκε αρχικά με απευθείας `docker run` (προηγούμενο session), οπότε του έλειπαν τα 3 labels (`net.unraid.docker.managed`, `.icon`, `.webui`) που έχουν όλα τα πραγματικά Unraid-managed containers (επιβεβαιώθηκε συγκρίνοντας με το EmbyServer/HOME-Omada — τα δύο που όντως εγκατέστησε ο Tommy μέσω Unraid· το HomeAssistant αποδείχτηκε επίσης χειροκίνητο, άκυρο reference point). Πλήρες backup του live config πριν το recreate: `docs/unraid-deployment-notes.md`. Recreate μέσω SSH με το ίδιο production config (ports/volumes/env vars) plus τα 3 labels plus το τελευταίο image — επιβεβαιωμένο: revision label = HEAD, `/health` OK, όλοι οι λογαριασμοί + το HA integration επιβίωσαν (persistent volume, καμία απώλεια δεδομένων). Documented επίσης το NginxProxyManager (2 proxy hosts: `athena.local` χωρίς SSL, `tommyathenad.duckdns.org` με SSL) και το duckdns-updater setup.
